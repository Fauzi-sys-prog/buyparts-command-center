services:
  postgres:
    image: postgres:16-alpine
    container_name: buyparts-postgres
    environment:
      POSTGRES_DB: buyparts
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - ./postgres-data:/var/lib/postgresql/data
